# Nix build results
result
result-*

# Hardware configuration (system-specific, but allow templates in hosts/)
/hardware-configuration.nix

# Backup files
*.bak
*.old

# Temporary files
.#*
*~

# Certificates and sensitive config
.config/

# Old overlays and configurations we replaced
overlays-6.1/
overlays/i915-sriov-dkms.nix.bak
overlays/intel-gfx-sriov.nix.bak

# Legacy files in root directory (not modules/)
/configuration.nix
/boot.nix
/graphics.nix
/remote-build.nix

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: disk-arch-nfs-pvc
spec:
  volumeName: disk-arch-nfs
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: nfs
  resources:
    requests:
      storage: 1Ti

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: disk-fedora-nfs-pvc
spec:
  volumeName: disk-fedora-nfs
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: nfs
  resources:
    requests:
      storage: 1Ti

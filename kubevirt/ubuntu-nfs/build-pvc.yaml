apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: disk-ubuntu-nfs-pvc
spec:
  volumeName: disk-ubuntu-nfs
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: nfs
  resources:
    requests:
      storage: 1Ti

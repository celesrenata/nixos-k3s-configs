apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariabackup
spec:
  volumeName: mariabackup
  storageClassName: ""
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
status: {}

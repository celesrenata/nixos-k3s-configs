apiVersion: v1
kind: PersistentVolume
metadata:
  name: startup-wikijs-data
spec:
  persistentVolumeReclaimPolicy: Retain
  capacity:
    storage: 40Gi
  accessModes:
    - ReadWriteOnce
    - ReadWriteMany
  storageClassName: ""
  nfs:
    path: /volume1/Kubernetes/startup/wikijs/data     # The path to your media
    server: 192.168.42.8     # Your NFS server with Media
  mountOptions:
    - vers=4
    - minorversion=1
    - noac

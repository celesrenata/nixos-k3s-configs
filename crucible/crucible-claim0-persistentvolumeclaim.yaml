apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: crucible-claim0
  name: crucible-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

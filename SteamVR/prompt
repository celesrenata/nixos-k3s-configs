we are working on steamvr container, we are in the process of fixing the xrdp pipewire plugin. Please explore the directory to get an understanding of the project. you should also view the plugin we're writing `~/sources/celesrenata/pipewire-module-xrdp` as we will be working on it most of the time. Please familiarize yourself with the pipewire documentation `https://docs.pipewire.org/1.4/` `https://docs.pipewire.org/1.4/page_modules.html` `https://docs.pipewire.org/1.4/topics.html` we were previously writing the plugin for 1.0.5, I would instead upgrade it to work for 1.4! we will need to build on `celes@192.168.42.254` as this laptop is very slow. once done, we can upload it to the local git server (see `~/sources/kube/git-server`) and then deploy from there to the kube cluser accessible from `kubectl` locally. once you have completed your review process of each source, please write a context file in a dedicated context directory as we will be using what you find and research in relation to our current module in `~/sources/celesrenata/pipewire-module-xrdp`
I'll explore the project structure and familiarize myself with the codebase. Let me start by 
examining the current directory and then the pipewire module you're working on.
